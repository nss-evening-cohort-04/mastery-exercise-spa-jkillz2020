var CarLot = (function(originalCarLot) {
  //originalCarLot.carsToDom = function(populateCars) {
    // var carList = document.getElementsByClassName("container"[0])
    

  // Loop over the inventory and populate the page
// function populateCars (inventory) {
//   var output = "";
//   for (var i = 0; i < inventory.length; i++) {
//     var currentCar = inventory[i]
//   console.log(inventory);

//     output += "<div>currentCar.make + currentCar.model</div>";
//   }
// }

  // Now that the DOM is loaded, establish all the event listeners needed
//   CarLot.activateEvents();
//   document.getElementsByClassName("container").addEventListener("click")
// };

// Load the inventory and send a callback function to be
// invoked after the process is complete
// CarLot.loadCars();

//return originalCarLot;

})(CarLot || {});